# -*- mode:sh -*-
# vim:syn=sh

BINDIR="${BASEDIR}/bin"
CONFDIR="${BASEDIR}/etc"
LOCKDIR="${BASEDIR}/locks"
LOGDIR="${BASEDIR}/log"

VERSION="g$(git rev-parse --short HEAD 2>/dev/null || echo "unknown")"

. "${BASEDIR}/bin/common"
